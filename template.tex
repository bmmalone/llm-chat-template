\documentclass[11pt, oneside]{article}

% --- Essential Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{setspace}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{hyperref}

% --- Layout & Margins ---
% 1.25 inch margins feel more like a "draft" or "journal"
\geometry{
    letterpaper,
    margin=1.25in,
    footskip=0.5in
}

% --- Typography ---
% Using Noto Serif to match your Makefile's mainfont
\usepackage{noto}
\onehalfspacing % Better readability for long LLM responses

% --- Colors & Styling ---
\definecolor{popcolor}{HTML}{2D88A8} % A more readable "Dark Gold" for print

% Style the Section Headers (H1, H2, H3)
\titleformat{\section}{\large\bfseries\sffamily}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries\color{popcolor}}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\itshape\color{popcolor}}{\thesubsubsection}{1em}{}

% --- Hyperlink Styling ---
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=popcolor,
}

% Add these lines to capture the YAML metadata
$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \\ $endfor$}
$endif$
$if(date)$
\date{$date$}
$endif$

% --- The Content ---
\begin{document}

% This command actually prints the title block onto the page
$if(title)$
\maketitle
$endif$

% Removes the indent from the start of paragraphs (prose style)
\setlength{\parindent}{0pt}
\setlength{\parskip}{1em}

$body$

\end{document}